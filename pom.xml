<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>

	<groupId>io.github.dfa1.lmdb</groupId>
	<artifactId>lmdb-java</artifactId>
	<version>0.1-SNAPSHOT</version>
	<packaging>pom</packaging>

	<name>lmdb-java</name>
	<description>Java FFM (Foreign Function &amp; Memory) bindings for LMDB</description>
	<url>https://github.com/dfa1/lmdb-java</url>

	<licenses>
		<license>
			<name>The OpenLDAP Public License, Version 2.8</name>
			<url>https://www.openldap.org/software/release/license.html</url>
			<distribution>repo</distribution>
		</license>
	</licenses>

	<developers>
		<developer>
			<id>dfa1</id>
			<name>Davide Angelocola</name>
			<url>https://github.com/dfa1</url>
		</developer>
	</developers>

	<scm>
		<connection>scm:git:https://github.com/dfa1/lmdb-java.git</connection>
		<developerConnection>scm:git:git@github.com:dfa1/lmdb-java.git</developerConnection>
		<url>https://github.com/dfa1/lmdb-java</url>
		<tag>HEAD</tag>
	</scm>

	<modules>
		<module>native/osx-aarch64</module>
		<module>native/osx-x86_64</module>
		<module>native/linux-x86_64</module>
		<module>native/linux-aarch64</module>
		<module>native/windows-x86_64</module>
		<module>native/windows-aarch64</module>
		<module>lmdb</module>
		<module>lmdb-platform</module>
		<module>bom</module>
	</modules>

	<properties>
		<project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
		<maven.compiler.release>25</maven.compiler.release>
		<maven-source-plugin.version>3.4.0</maven-source-plugin.version>
		<maven-javadoc-plugin.version>3.12.0</maven-javadoc-plugin.version>
		<maven-jar-plugin.version>3.5.1</maven-jar-plugin.version>
		<jacoco-maven-plugin.version>0.8.15</jacoco-maven-plugin.version>

		<!-- Default for the @{argLine} placeholder in the surefire config below.
		     JaCoCo's prepare-agent overwrites this property with the agent args when
		     the coverage profile is active; the empty default keeps surefire from
		     receiving a literal @{argLine} token when it is not. -->
		<argLine />
	</properties>

	<dependencyManagement>
		<dependencies>
			<dependency>
				<groupId>io.github.dfa1.lmdb</groupId>
				<artifactId>lmdb</artifactId>
				<version>${project.version}</version>
			</dependency>
			<dependency>
				<groupId>io.github.dfa1.lmdb</groupId>
				<artifactId>lmdb-platform</artifactId>
				<version>${project.version}</version>
			</dependency>
			<dependency>
				<groupId>io.github.dfa1.lmdb</groupId>
				<artifactId>lmdb-native-osx-aarch64</artifactId>
				<version>${project.version}</version>
			</dependency>
			<dependency>
				<groupId>io.github.dfa1.lmdb</groupId>
				<artifactId>lmdb-native-osx-x86_64</artifactId>
				<version>${project.version}</version>
			</dependency>
			<dependency>
				<groupId>io.github.dfa1.lmdb</groupId>
				<artifactId>lmdb-native-linux-x86_64</artifactId>
				<version>${project.version}</version>
			</dependency>
			<dependency>
				<groupId>io.github.dfa1.lmdb</groupId>
				<artifactId>lmdb-native-linux-aarch64</artifactId>
				<version>${project.version}</version>
			</dependency>
			<dependency>
				<groupId>io.github.dfa1.lmdb</groupId>
				<artifactId>lmdb-native-windows-x86_64</artifactId>
				<version>${project.version}</version>
			</dependency>
			<dependency>
				<groupId>io.github.dfa1.lmdb</groupId>
				<artifactId>lmdb-native-windows-aarch64</artifactId>
				<version>${project.version}</version>
			</dependency>
			<dependency>
				<groupId>org.junit.jupiter</groupId>
				<artifactId>junit-jupiter</artifactId>
				<version>6.1.2</version>
			</dependency>
			<dependency>
				<groupId>org.assertj</groupId>
				<artifactId>assertj-core</artifactId>
				<version>3.27.7</version>
			</dependency>
		</dependencies>
	</dependencyManagement>

	<build>
		<pluginManagement>
			<plugins>
				<plugin>
					<groupId>org.apache.maven.plugins</groupId>
					<artifactId>maven-compiler-plugin</artifactId>
					<version>3.15.0</version>
					<configuration>
						<release>25</release>
						<failOnWarning>true</failOnWarning>
						<compilerArgs>
							<arg>-Xlint:all</arg>
						</compilerArgs>
					</configuration>
				</plugin>
				<plugin>
					<groupId>org.apache.maven.plugins</groupId>
					<artifactId>maven-javadoc-plugin</artifactId>
					<version>3.12.0</version>
					<configuration>
						<release>25</release>
						<failOnError>true</failOnError>
						<failOnWarnings>true</failOnWarnings>
						<quiet>true</quiet>
						<!-- Don't cross-link to sibling reactor modules: the native/bom
						     modules have no Java sources, so link detection just creates
						     fake apidocs dirs and logs "Error fetching link". -->
						<detectOfflineLinks>false</detectOfflineLinks>
					</configuration>
				</plugin>
				<plugin>
					<groupId>org.apache.maven.plugins</groupId>
					<artifactId>maven-surefire-plugin</artifactId>
					<version>3.5.6</version>
					<configuration>
						<!-- @{argLine} carries JaCoCo's agent when the coverage profile is
						     active (empty otherwise); the native-access flag is always on. -->
						<argLine>@{argLine} --enable-native-access=ALL-UNNAMED</argLine>
					</configuration>
				</plugin>
				<plugin>
					<groupId>org.jacoco</groupId>
					<artifactId>jacoco-maven-plugin</artifactId>
					<version>${jacoco-maven-plugin.version}</version>
				</plugin>
				<plugin>
					<groupId>org.codehaus.mojo</groupId>
					<artifactId>exec-maven-plugin</artifactId>
					<version>3.6.3</version>
				</plugin>
				<plugin>
					<groupId>org.apache.maven.plugins</groupId>
					<artifactId>maven-checkstyle-plugin</artifactId>
					<version>3.6.0</version>
					<executions>
						<execution>
							<phase>validate</phase>
							<goals>
								<goal>check</goal>
							</goals>
							<configuration>
								<configLocation>${maven.multiModuleProjectDirectory}/checkstyle.xml</configLocation>
								<failsOnError>true</failsOnError>
								<consoleOutput>true</consoleOutput>
								<sourceDirectories>
									<sourceDirectory>src/main/java</sourceDirectory>
									<sourceDirectory>src/test/java</sourceDirectory>
								</sourceDirectories>
								<excludes>**/module-info.java</excludes>
							</configuration>
						</execution>
					</executions>
					<dependencies>
						<dependency>
							<groupId>com.puppycrawl.tools</groupId>
							<artifactId>checkstyle</artifactId>
							<version>13.9.0</version>
						</dependency>
					</dependencies>
				</plugin>
				<plugin>
					<groupId>org.apache.maven.plugins</groupId>
					<artifactId>maven-jar-plugin</artifactId>
					<version>${maven-jar-plugin.version}</version>
					<executions>
						<!-- Resource-only native modules carry no Java sources. Attach
						     empty javadoc/sources jars (from non-existent dirs) so Maven
						     Central's "javadoc + sources required" validation passes. -->
						<execution>
							<id>empty-javadoc-jar</id>
							<phase>package</phase>
							<goals>
								<goal>jar</goal>
							</goals>
							<configuration>
								<classifier>javadoc</classifier>
								<classesDirectory>${project.build.directory}/empty-javadoc</classesDirectory>
								<forceCreation>true</forceCreation>
							</configuration>
						</execution>
						<execution>
							<id>empty-sources-jar</id>
							<phase>package</phase>
							<goals>
								<goal>jar</goal>
							</goals>
							<configuration>
								<classifier>sources</classifier>
								<classesDirectory>${project.build.directory}/empty-sources</classesDirectory>
								<forceCreation>true</forceCreation>
							</configuration>
						</execution>
					</executions>
				</plugin>
			</plugins>
		</pluginManagement>
		<plugins>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-checkstyle-plugin</artifactId>
			</plugin>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-javadoc-plugin</artifactId>
				<executions>
					<execution>
						<id>javadoc-no-warnings</id>
						<phase>verify</phase>
						<goals>
							<goal>javadoc</goal>
						</goals>
					</execution>
				</executions>
			</plugin>
		</plugins>
	</build>

	<profiles>
		<!-- ./mvnw verify -P coverage
		     Attaches the JaCoCo agent to surefire (via the @{argLine} placeholder)
		     and writes one target/site/jacoco/jacoco.xml per module. -->
		<profile>
			<id>coverage</id>
			<build>
				<plugins>
					<plugin>
						<groupId>org.jacoco</groupId>
						<artifactId>jacoco-maven-plugin</artifactId>
						<executions>
							<execution>
								<id>prepare-agent</id>
								<goals>
									<goal>prepare-agent</goal>
								</goals>
							</execution>
							<execution>
								<id>report</id>
								<phase>verify</phase>
								<goals>
									<goal>report</goal>
								</goals>
							</execution>
						</executions>
					</plugin>
				</plugins>
			</build>
		</profile>
	</profiles>
</project>
